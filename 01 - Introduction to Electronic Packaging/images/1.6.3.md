<!-- MDBOOK-EXCLUDE-START -->
## Suggested Content for Figure 1.6.3

**Instructional purpose:**  
Show how basic logic gates combine to create a useful digital functional circuit.

**Filename:**  
`1.6.3 - From Logic Gates to a Half-Adder.png`

**Intended width:** 900 px

**Recommended aspect ratio:** 16:9

### Required layout

Use a clean left-to-right controlled-vector logic diagram.

Show:

- input `A`;
- input `B`;
- XOR gate;
- AND gate;
- `SUM (S)` output;
- `CARRY (C)` output.

Both inputs must feed both gates.

### Required relationships

- XOR output → `SUM (S)`.
- AND output → `CARRY (C)`.

The figure must agree with:

`SUM = A XOR B`

`CARRY = A AND B`

### Optional secondary progression

If space permits, add a small secondary callout:

`Half-adder → Full adder → Multi-bit adder`

Do not show detailed full-adder circuitry.

### Visual style

- conventional logic-gate symbols;
- clean 2D controlled-vector diagram;
- large labels;
- ample spacing;
- white/light background;
- no embedded overall title.

### Prohibited or misleading elements

- Do not substitute OR for XOR.
- Do not swap SUM and CARRY.
- Do not imply a half-adder has a carry input.
- Do not depict logic-gate symbols as packaged components inside an IC.

### Verification sources

- earlier Chapter 1 instructional materials and prior Chapter 1 draft.
- Current Section 1.6.
- Current project and image-generation rules.

### Final caption

*Figure 1.6.3 – A half-adder combines an XOR gate and an AND gate to convert two one-bit inputs into SUM and CARRY outputs.*

### Alt-text intent

Describe A and B branching to an XOR gate that produces SUM and an AND gate that produces CARRY.

**Provenance action after figure creation:**  
Record the final figure, sources, generation method, originality status, iteration history, and technical-review result in `source_provenance.md`.

<!-- MDBOOK-EXCLUDE-END -->