## Suggested Content for Figure 1.6.7

**Figure Title:** CMOS OR Function Using NOR and Inverter

**Purpose:**  
Show how a non-inverting OR function can be constructed from a NOR stage followed by an inverter.

**Suggested Content:**  
Create a clean left-to-right functional circuit:

`A, B → CMOS NOR stage → intermediate node X → CMOS inverter → output Y`

Required labels:

- input $A$;
- input $B$;
- `NOR`;
- intermediate node $X$;
- `Inverter`;
- output $Y$.

Show the equations:

- $X=\overline{A+B}$;
- $Y=\overline{X}=A+B$.

The NOR block may use a simplified transistor-level drawing or a clearly labeled gate/block representation. Do not repeat the full inverter lesson.

**Style Requirements:**
- uncluttered left-to-right flow;
- clearly separate the NOR stage from the inverter stage;
- no embedded figure number or overall title.